import {TracksDurationPipe} from './tracks-duration.pipe';

describe('TracksDurationPipe', () => {
  it('create an instance', () => {
    const pipe = new TracksDurationPipe();
    expect(pipe).toBeTruthy();
  });
});
