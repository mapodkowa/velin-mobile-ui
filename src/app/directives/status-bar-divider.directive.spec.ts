import {StatusBarDividerDirective} from './status-bar-divider.directive';

describe('StatusBarDividerDirective', () => {
  it('should create an instance', () => {
    const directive = new StatusBarDividerDirective();
    expect(directive).toBeTruthy();
  });
});
